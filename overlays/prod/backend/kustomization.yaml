apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app-prod

resources:
  - ../../../base/backend

images:
  - name: selab-backend            # ← 必須對應 base/backend.yaml 內的占位 image 名稱
    newName: kevin0413/selab-backend
    newTag: "1.0.0"
